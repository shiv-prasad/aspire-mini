export enum RequestMethods {
    GET = 'GET',
    POST = 'POST',
    PATCH = 'PATCH',
}

export enum HttpStatusCodes {
    OK = 200,
    ACCEPTED = 202,
    NO_CONTENT = 204,
    NOT_ACCEPTABLE = 406,
    INTERNAL_SERVER_ERROR = 500,
    BAD_REQUEST = 400,
    NOT_FOUND = 404,
    UNAUTHORIZED = 401
}
